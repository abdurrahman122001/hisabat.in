RewriteEngine On

# API and PHP endpoints
RewriteRule ^api/ - [L]
RewriteRule \.php$ - [L]

# Serve existing files/directories directly
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# SPA fallback
RewriteRule ^ index.html [L]
